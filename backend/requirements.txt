# Core dependencies
python-dotenv>=0.19.0
openai>=1.0.0
pydantic>=2.0.0
openai-agents

# Web framework support
flask>=2.3.0
flask-cors>=4.0.0

# Agent framework (Agentic AI SDK)

# Optional FastAPI support (if switching later)
fastapi>=0.68.0
uvicorn>=0.15.0

nltk>=3.6.0
textstat>=0.7.0

pandas>=1.3.0

# Configuration already included above